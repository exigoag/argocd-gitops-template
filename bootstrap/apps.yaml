---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  name: gitops-apps
  namespace: argocd
spec:
## IMPORTANT: enable preserveResourcesOnDeletion before changing anything in here to prevent unwanted uninstall/redeploy of apps, remove again afterwards
#  syncPolicy:
#    preserveResourcesOnDeletion: true
  goTemplate: true
  goTemplateOptions: ["missingkey=default"]
  generators:
    - git:
        repoURL: https://github.com/exigoag/argocd-gitops-template.git
        revision: "main"
        requeueAfterSeconds: 20
        files:
          - path: "apps/**/app.yaml"
        values:
          project: "{{index .path.segments 1}}"
          name: "{{index .path.segments 2}}"
          cluster: "{{index .path.segments 3}}"
          namespace: "{{index .path.segments 4}}"
          base_path: "apps/{{index .path.segments 1}}/{{index .path.segments 2}}/base"
          app_path: "{{.path.path}}"
  template:
    metadata:
      name: '{{ default .appName ( join "-" ( uniq ( splitList "-" ( join "-" ( compact ( list .values.cluster .values.project .values.namespace .values.name ) ) ) ) ) ) | lower }}'
      namespace: argocd
    spec:
      destination:
        namespace: '{{ default .values.namespace .destNamespace }}'
        name: '{{ default .values.cluster .destServer }}'
      project: '{{.values.project}}'
      syncPolicy:
        automated:
          allowEmpty: true
          prune: true
          selfHeal: true
        retry:
          limit: 30
          backoff:
            duration: 20s
            factor: 1
            maxDuration: 20s
        syncOptions:
          - CreateNamespace=true
          - DisableHelmChartCache=true
          - RespectIgnoreDifferences=true
  # language=gotemplate
  templatePatch: |
    metadata:
      labels:
        app.kubernetes.io/name: '{{.values.name}}'
        {{- range $key, $value := .labels }}
        {{ $key }}: {{ $value | quote }}
        {{- end }}
      annotations:
        {{- range $key, $value := .annotations }}
        {{ $key }}: {{ $value | quote }}
        {{- end }}
    spec:
      sources:
        - repoURL: '{{ default "https://github.com/exigoag/argocd-gitops-template.git" .repoURL }}'
          targetRevision: '{{ default "main" .srcTargetRevision }}'
          {{ if hasSuffix ".git" .repoURL }}
          path: '{{ default .values.name .srcChart }}'
          {{ else }}
          chart: '{{ default .values.name .srcChart }}'
          {{ end }}
          helm:
            releaseName: '{{ default .values.name .srcReleaseName }}'
            valueFiles:
              - "$repo/apps/{{ .values.project }}/base/values.yaml"
              - "$repo/{{ .values.base_path }}/values.yaml"
              - "$repo/{{ .values.app_path }}/values.yaml"
            ignoreMissingValueFiles: true
        - repoURL: "https://github.com/exigoag/argocd-gitops-template.git"
          targetRevision: main
          ref: repo
      {{ with .ignoreDifferences }}
      ignoreDifferences:
        {{ toYaml . | nindent 8 }}
      {{ end }}          
  ignoreApplicationDifferences:
    - jqPathExpressions:
        - .spec.sources[] | select(has("helm")).helm.parameters